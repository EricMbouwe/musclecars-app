import AppointmentList from '../components/AppointmentList';

const Appointments = () => {
  const test = 'hey';

  return (
    <div>
      <p>{test}</p>
      <AppointmentList />
    </div>
  );
};

export default Appointments;
