const Appointments = () => {
  const appointments = [];

  return (
    <div>
      <h2>
        The appointments of the app-
        {appointments}
      </h2>
    </div>
  );
};

export default Appointments;
