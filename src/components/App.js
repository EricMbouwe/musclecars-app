import { Route, Router, Switch } from 'react-router';
import './App.css';

function App() {
  return (
    <div className="App">
      <Router>
        <Switch>
          <Route exact path="/" component={} />
          <Route exact path="/" component={} />
        </Switch>
      </Router>
    </div>
  );
}

export default App;
